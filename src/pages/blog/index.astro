---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import config from '../../content/config/config.json';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);

---

<!doctype html>
<html lang="tr">
	<head>
		<BaseHead title="Dokümanlar - Kadıköy Kooperatifi Arşivi" description="Kadıköy Kooperatifi'nin tüzük, yönetmelik ve toplantı tutanakları" />
		<style>
			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 2rem 1rem;
			}
			
			.page-header {
				text-align: center;
				margin-bottom: 3rem;
				padding: 2rem 0;
				background: linear-gradient(135deg, #1a4d2e 0%, #4ade80 100%);
				color: white;
				border-radius: 12px;
			}
			
			.page-header h1 {
				font-size: 2.5rem;
				margin: 0 0 1rem 0;
			}
			
			.page-header p {
				font-size: 1.2rem;
				margin: 0;
				opacity: 0.9;
			}
			
			.categories {
				display: flex;
				justify-content: center;
				gap: 1rem;
				margin-bottom: 2rem;
				flex-wrap: wrap;
			}
			
			.category-btn {
				padding: 0.5rem 1rem;
				background: var(--accent);
				color: white;
				border: none;
				border-radius: 25px;
				cursor: pointer;
				transition: 0.3s ease;
				font-size: 0.9rem;
			}
			
			.category-btn:hover {
				background: #1a4d2e;
				transform: translateY(-2px);
			}
			
			.category-btn.active {
				background: #1a4d2e;
			}
			
			ul {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			
			ul li {
				background: white;
				border-radius: 12px;
				overflow: hidden;
				box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
				transition: 0.3s ease;
			}
			
			ul li:hover {
				transform: translateY(-5px);
				box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
			}
			
			ul li a {
				display: block;
				text-decoration: none;
				color: inherit;
			}
			
			ul li img {
				width: 100%;
				height: 200px;
				object-fit: cover;
				margin: 0;
			}
			
			.document-info {
				padding: 1.5rem;
			}
			
			.title {
				margin: 0 0 0.5rem 0;
				color: var(--accent);
				font-size: 1.3rem;
				line-height: 1.3;
			}
			
			.description {
				margin: 0 0 1rem 0;
				color: #666;
				font-size: 0.95rem;
				line-height: 1.5;
			}
			
			.date {
				margin: 0;
				color: #888;
				font-size: 0.85rem;
				font-weight: 500;
			}
			
			.document-type {
				display: inline-block;
				padding: 0.25rem 0.75rem;
				background: #e8f5e8;
				color: #1a4d2e;
				border-radius: 15px;
				font-size: 0.8rem;
				font-weight: 600;
				margin-bottom: 0.5rem;
			}
			
			@media (max-width: 720px) {
				main {
					padding: 1rem;
				}
				
				.page-header h1 {
					font-size: 2rem;
				}
				
				.page-header p {
					font-size: 1rem;
				}
				
				ul {
					grid-template-columns: 1fr;
					gap: 1rem;
				}
				
				.categories {
					gap: 0.5rem;
				}
				
				.category-btn {
					padding: 0.4rem 0.8rem;
					font-size: 0.8rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div class="page-header">
				<h1>📚 Dokümanlar</h1>
				<p>Kadıköy Kooperatifi'nin tüzük, yönetmelik ve toplantı tutanakları</p>
			</div>
			
			<div class="categories">
				<button class="category-btn active" data-category="all">Tümü</button>
				<button class="category-btn" data-category="tuzuk">Tüzük & Yönetmelik</button>
				<button class="category-btn" data-category="toplanti">Toplantı Tutanakları</button>
				<button class="category-btn" data-category="egitim">Eğitim Materyalleri</button>
			</div>
			
			<section>
				<ul>
					{
						posts.map((post) => (
							<li data-category={post.data.title.includes('Tüzük') || post.data.title.includes('Yönetmelik') ? 'tuzuk' : 
												 post.data.title.includes('Toplantı') || post.data.title.includes('Tutanak') ? 'toplanti' : 
												 post.data.title.includes('Eğitim') ? 'egitim' : 'other'}>
								<a href={`/blog/${post.id}/`}>
									<img width={720} height={360} src={post.data.heroImage} alt="" />
									<div class="document-info">
										<span class="document-type">
											{post.data.title.includes('Tüzük') || post.data.title.includes('Yönetmelik') ? '📋 Yönetim' :
											 post.data.title.includes('Toplantı') || post.data.title.includes('Tutanak') ? '📊 Toplantı' :
											 post.data.title.includes('Eğitim') ? '📚 Eğitim' : '📄 Doküman'}
										</span>
										<h4 class="title">{post.data.title}</h4>
										<p class="description">{post.data.description}</p>
										<p class="date">
											<FormattedDate date={post.data.pubDate} />
										</p>
									</div>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
		
		<script>
			// Kategori filtreleme
			const categoryBtns = document.querySelectorAll('.category-btn');
			const documentItems = document.querySelectorAll('ul li');
			
			categoryBtns.forEach(btn => {
				btn.addEventListener('click', () => {
					const category = btn.dataset.category;
					
					// Aktif buton güncelleme
					categoryBtns.forEach(b => b.classList.remove('active'));
					btn.classList.add('active');
					
					// Dokümanları filtreleme
					documentItems.forEach(item => {
						if (category === 'all' || item.dataset.category === category) {
							item.style.display = 'block';
						} else {
							item.style.display = 'none';
						}
					});
				});
			});
		</script>
	</body>
</html>
